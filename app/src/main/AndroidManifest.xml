<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alexfaber.sumanalarm" >

    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"></uses-permission>
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:name="ApplicationController"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.Settings"
            android:label="@string/title_activity_settings" >
        </activity>
        <activity
            android:name=".Activities.Challenges"
            android:label="@string/title_activity_challenges" >
        </activity>
        <activity
            android:name=".Activities.AlarmActivity"
            android:label="@string/title_activity_alarm" >
        </activity>

        <service
            android:name=".MyWakefulService"
            android:label="MyWakefulService" >
            android:enabled="true"
            <intent-filter>
                <action android:name="android.intent.action.MYWAKEFULSERVICE" />
            </intent-filter>
        </service>

        <receiver android:name=".AlarmReceiver" >
        </receiver>

        <activity
            android:name=".Activities.CreateChallenge"
            android:label="@string/title_activity_create_challenge" >
        </activity>
    </application>

</manifest>
